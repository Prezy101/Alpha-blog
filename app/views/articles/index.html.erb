<center><h1>Listing all articles</h1>


  <table>

    <tr>
      <th>Edit</th>
      <th>Title</th>
      <th>Description</th>
      <th>Created</th>
      <th>Show</th>

    </tr>

    <tr>
      <% @article.each do |art| %>
          <td><%= link_to art.id, edit_article_url(art) %></td>
          <td><%= art.title %></td>
          <td><%= art.description %></td>
          <td><%= art.created_at %></td>
          <td><%= link_to 'show', article_path(art) %></td>

          </tr>

    <% end %>

  </table>
  <br/>
  <%= link_to 'Create new article', new_article_url %>

</center>